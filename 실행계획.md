# DailyWorkAlter 실행계획

퇴근 30분 전에 자동으로 떠서 오늘 한 일을 기록하게 하고, 주 1회 지정한 요일에는
그 주 전체를 펼쳐 보여주는 Windows 트레이 상주 앱.

---

## 1. 확정된 결정사항

| 항목 | 결정 | 근거 |
|---|---|---|
| 스택 | **Tauri 2** (Rust + HTML/CSS/JS) | 상주 RAM 최소, 단일 6MB exe, UI는 웹이라 유지보수 쉬움 |
| UI 프레임워크 | **없음 (vanilla JS)** | 화면 4개 규모. React 도입 시 빌드 복잡도만 늘어남 |
| 데이터 위치 | `%APPDATA%\DailyWorkAlter\` | 표준 관례, 숨김 폴더라 실수 삭제 위험 낮음, exe 재빌드와 무관 |
| 로그 포맷 | 하루 1개 마크다운 파일 | 앱 없이도 읽기/검색 가능, 이관 자유 |
| 동기화/백업 | **없음 (순수 로컬)** | 사용자 요청 |
| 로그 버전관리 | **없음** | 사용자 요청. git은 소스코드 전용 |
| 자동 입력(git 커밋 수집) | **1단계 제외** | 수동 입력으로 먼저 써보고 판단 |

### 설계상 중요한 정정

당초 "Rust는 30줄"로 예상했으나, **상주 RAM 20MB를 지키려면 스케줄러가 Rust에 있어야 한다.**

- 창을 숨기지(hide) 않고 **파기(destroy)** 해야 WebView2 프로세스가 내려가고 RAM이 회수됨
- 창이 없으면 JS가 죽으므로, 타이머를 JS에 두면 알림이 아예 안 뜸
- 따라서 **타이머·설정·파일 IO는 전부 Rust**, JS는 화면 렌더링만 담당

결과적으로 Rust 코드는 300~400줄 규모. 대신 하루 23.5시간 동안 상주 메모리는
Rust 프로세스 하나(약 15~25MB)뿐.

> 대안: 창을 숨기기만 하면 Rust는 최소화되지만 상주 RAM이 80MB대로 오름.
> RAM이 우선 조건이므로 위 구조를 채택.

---

## 2. 데이터 설계

### 2.1 디렉터리

```
C:\Users\USER\AppData\Roaming\DailyWorkAlter\
├─ config.json          설정 (사용자가 직접 편집 가능)
├─ logs\
│   ├─ 2026-09-07.md
│   ├─ 2026-09-08.md
│   └─ ...
└─ state.json           앱 내부 상태 (스누즈 시각, 마지막 알림일 등)
```

`config.json`과 `state.json`을 분리하는 이유: 사용자가 직접 여는 파일에 앱 내부 상태가
섞이면 수정하다 깨뜨리기 쉽다. 사용자가 신경 쓸 파일은 `config.json` 하나뿐.

### 2.2 config.json 스키마

```json
{
  "version": 1,
  "work": {
    "startTime": "09:00",
    "endTime": "18:00",
    "workdays": [1, 2, 3, 4, 5]
  },
  "notify": {
    "minutesBefore": 30,
    "snoozeMinutes": 10,
    "catchUpHours": 4
  },
  "weekly": {
    "enabled": true,
    "mode": "fixedDay",
    "day": 5
  },
  "hotkey": "CommandOrControl+Alt+D",
  "autostart": true,
  "logDir": null
}
```

| 키 | 설명 |
|---|---|
| `work.startTime` / `endTime` | 출근/퇴근 시각 `HH:MM`. 사용자 지정 |
| `work.workdays` | 근무 요일. **1=월 … 7=일** (ISO 8601) |
| `notify.minutesBefore` | 퇴근 몇 분 전에 띄울지. 기본 30 |
| `notify.snoozeMinutes` | 스누즈 간격 |
| `notify.catchUpHours` | 절전 등으로 알림 시각을 놓쳤을 때 몇 시간까지 늦게라도 띄울지 |
| `weekly.mode` | `fixedDay`(지정 요일) 또는 `lastWorkday`(그 주 마지막 근무일) |
| `weekly.day` | `fixedDay`일 때의 요일. 기본 5(금) |
| `hotkey` | 전역 단축키 |
| `autostart` | 로그인 시 자동 실행 |
| `logDir` | `null`이면 기본 경로. 문자열이면 그 절대경로 사용 |

**출근 시간의 용도** — 알림이 엉뚱한 시간에 뜨는 것을 막는 가드. 절전에서 깨어났을 때
"아직 출근 전 시간대"면 띄우지 않는다. (향후 근무시간 통계에도 사용)

### 2.3 로그 파일 포맷

`logs/2026-09-07.md`

```markdown
---
date: 2026-09-07
weekday: 월
created: 2026-09-07T17:31:04+09:00
updated: 2026-09-07T17:38:22+09:00
---

- OCR 파이프라인 전처리 단계 리팩터링
- 배치 큐 타임아웃 원인 파악 (재시도에서 커넥션 반환 누락)
- 주간 배포 리뷰 참석
```

- 본문은 `- ` 불릿 목록. 파싱은 단순 라인 분해
- frontmatter는 앱이 관리하되, 사용자가 손으로 고쳐도 깨지지 않게 **관대하게 파싱**한다
  (frontmatter가 없으면 본문 전체를 내용으로 간주)

### 2.4 state.json

```json
{
  "lastNotifiedDate": "2026-09-07",
  "snoozeUntil": null,
  "skippedDates": ["2026-09-04"],
  "lastTickAt": "2026-09-07T17:30:00+09:00"
}
```

`lastTickAt`은 절전 복귀 감지에 사용한다 (3.2 참고).

---

## 3. 스케줄러 로직 (Rust)

### 3.1 기본 루프

60초마다 tick. 매 tick에서 아래를 순서대로 판정하고, 하나라도 걸리면 중단.

```
1. 오늘이 work.workdays 에 없다              → 종료
2. 오늘이 state.skippedDates 에 있다         → 종료
3. 오늘 로그 파일이 이미 있고 내용이 있다     → 종료
4. state.lastNotifiedDate == 오늘            → 종료 (이미 띄웠음)
5. snoozeUntil 이 미래다                     → 종료
6. 지금 >= (endTime - minutesBefore) 이고
   지금 <= (endTime + catchUpHours)          → 창 띄우기
```

### 3.2 절전 / 재부팅 복귀 처리

Windows 절전 이벤트를 직접 구독하지 않고, **tick 간격이 벌어진 것으로 감지**한다.

```
now - state.lastTickAt > 5분  →  절전이었거나 앱이 꺼져 있었음
                              →  즉시 3.1 판정을 1회 수행
```

별도 API 의존 없이 재부팅·앱 재시작·절전을 한 번에 커버한다.
단 아직 출근 전(`now < startTime`)이면 띄우지 않는다.

### 3.3 주간 모드 판정

```
weekly.enabled == false                        → 일일 모드
mode == "fixedDay"   && 오늘 요일 == day        → 주간 모드
mode == "lastWorkday" && 오늘이 이번 주 마지막 근무일 → 주간 모드
그 외                                          → 일일 모드
```

주간 모드여도 **오늘 입력란은 그대로 있다.** 주간 목록이 위에 추가로 붙는 형태.

### 3.4 스누즈 / 건너뛰기

- **스누즈**: `snoozeUntil = now + snoozeMinutes`, 창 파기
- **오늘 건너뛰기**: `skippedDates`에 오늘 추가, 창 파기
- `skippedDates`는 30일 지난 항목을 자동 정리

---

## 4. 화면 설계

창은 **단일 윈도우**이고 내부에서 뷰만 교체한다.
크기 약 620×560, 화면 우하단 고정, 항상 위, 작업표시줄에는 표시하지 않음.

### 4.1 일일 입력 (기본)

```
┌──────────────────────────────────────────┐
│  2026-09-07 (월)                    ⚙ ✕  │
├──────────────────────────────────────────┤
│  오늘 한 일                              │
│  ┌────────────────────────────────────┐  │
│  │ - OCR 전처리 리팩터링              │  │
│  │ - 배치 큐 타임아웃 원인 파악       │  │
│  │ - |                                │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ▸ 어제 (09-06) 참고                     │
│                                          │
│  [10분 뒤 다시] [오늘 건너뛰기] [저장]   │
└──────────────────────────────────────────┘
```

- 텍스트영역 진입 시 `- ` 자동 삽입, Enter를 치면 다음 줄에도 `- ` 자동
- **입력 중 500ms 디바운스 자동저장** (강제 종료·정전 대비)
- "어제 참고"는 접힌 상태. 펼치면 어제 작성분을 읽기 전용으로 표시
- `Ctrl+Enter` = 저장 후 닫기, `Esc` = 저장 후 닫기

### 4.2 주간 요약 (지정 요일)

```
┌──────────────────────────────────────────┐
│  2026년 37주차 (09-07 ~ 09-11)      ⚙ ✕  │
├──────────────────────────────────────────┤
│  월 09-07  · OCR 전처리 리팩터링         │
│            · 배치 큐 타임아웃 원인 파악  │
│  화 09-08  · API 응답 스키마 정리        │
│  수 09-09  (작성 없음)                   │
│  목 09-10  · 배포 스크립트 수정          │
│  ──────────────────────────────────────  │
│  금 09-11  오늘 한 일                    │
│  ┌────────────────────────────────────┐  │
│  │ - |                                │  │
│  └────────────────────────────────────┘  │
│                                          │
│  [주간 전체 복사]       [저장하고 닫기]  │
└──────────────────────────────────────────┘
```

- 과거 요일은 클릭하면 인라인 편집 가능 (빠뜨린 것 보충용)
- **[주간 전체 복사]** → 아래 형식으로 클립보드에 복사

```markdown
## 2026년 37주차 (09-07 ~ 09-11)

### 09-07 (월)
- OCR 전처리 리팩터링
- 배치 큐 타임아웃 원인 파악

### 09-08 (화)
- API 응답 스키마 정리
```

### 4.3 설정 팝업

톱니(⚙) 또는 트레이 우클릭 → 설정. 같은 창 안의 모달로 띄운다.

```
┌──────────────────────────────────────────────┐
│  설정                                        │
├──────────────────────────────────────────────┤
│  출근 시간      [ 09:00 ]                    │
│  퇴근 시간      [ 18:00 ]                    │
│  알림 시점      퇴근 [ 30 ] 분 전            │
│                                              │
│  근무 요일      ☑월 ☑화 ☑수 ☑목 ☑금 ☐토 ☐일  │
│                                              │
│  주간 요약      ☑ 사용                       │
│                 ○ 지정 요일  [ 금 ▾ ]        │
│                 ○ 그 주 마지막 근무일        │
│                                              │
│  전역 단축키    [ Ctrl+Alt+D ]               │
│  스누즈 간격    [ 10 ] 분                    │
│  로그인 시 자동 실행  ☑                      │
│                                              │
│  [config.json 열기]   [로그 폴더 열기]       │
│                              [취소]  [저장]  │
└──────────────────────────────────────────────┘
```

- **[config.json 열기]** — 기본 편집기로 파일을 연다
- 저장 시 즉시 Rust로 전달하고, 스케줄러가 새 값으로 재계산한다
- 단축키 변경 시 기존 등록을 해제하고 재등록. 충돌하면 경고를 표시한다

### 4.4 config.json 직접 편집 지원

사용자가 파일을 손으로 고칠 수 있어야 하므로:

- Rust가 `config.json`을 **파일 감시(watch)** 하여 외부 변경 시 자동 리로드
- 파싱에 실패하면 **기존 설정을 유지**하고 트레이 알림으로 "설정 파일 오류"를 표시
  (잘못 고쳤다고 앱이 죽거나 설정이 초기화되면 안 된다)
- 누락된 키는 메모리상에서 기본값으로 채우되 **파일을 임의로 덮어쓰지 않는다**
  (사용자가 써넣은 포맷을 보존)

### 4.5 온보딩 (최초 1회)

첫 실행 시 설정 팝업을 축약한 화면을 띄운다.
출근/퇴근 시간과 주간 요약 요일만 물어보고 시작한다.

### 4.6 트레이 메뉴

```
지금 작성하기
이번 주 보기
──────────────
설정
로그 폴더 열기
──────────────
지금 알림 테스트     ← 개발·검증용
종료
```

---

## 5. 프로젝트 구조

```
DailyWorkAlter/
├─ 실행계획.md
├─ package.json
├─ index.html
├─ src/                       ← 프론트엔드 (화면 전담)
│   ├─ main.js                 진입점, 뷰 라우팅
│   ├─ styles.css
│   ├─ api.js                  Rust 커맨드 호출 래퍼
│   ├─ views/
│   │   ├─ daily.js
│   │   ├─ weekly.js
│   │   ├─ settings.js
│   │   └─ onboarding.js
│   └─ util/
│       ├─ date.js             요일·주차 계산
│       └─ markdown.js         불릿 파싱·직렬화
└─ src-tauri/
    ├─ Cargo.toml
    ├─ tauri.conf.json
    ├─ icons/
    └─ src/
        ├─ main.rs             앱 부트스트랩, 플러그인 등록
        ├─ config.rs           설정 로드·저장·watch·기본값 병합
        ├─ state.rs            state.json 관리
        ├─ storage.rs          로그 md 읽기·쓰기·주간 조회
        ├─ scheduler.rs        60초 tick 루프, 판정 로직
        ├─ window.rs           창 생성·파기, 위치 계산
        ├─ tray.rs             트레이 아이콘·메뉴
        └─ commands.rs         JS에 노출할 커맨드
```

### 5.1 Rust 커맨드 (JS ↔ Rust 인터페이스)

| 커맨드 | 설명 |
|---|---|
| `get_config()` | 설정 조회 |
| `save_config(config)` | 설정 저장 + 스케줄러 재계산 + 단축키 재등록 |
| `open_config_file()` | 기본 편집기로 config.json 열기 |
| `open_log_dir()` | 탐색기로 로그 폴더 열기 |
| `read_log(date)` | 특정 날짜 로그 읽기 |
| `write_log(date, items)` | 특정 날짜 로그 쓰기 |
| `read_week(anchor)` | 해당 주의 근무일 로그 일괄 조회 |
| `get_view_mode()` | 지금 띄울 화면이 daily인지 weekly인지 |
| `snooze()` | 스누즈 설정 후 창 파기 |
| `skip_today()` | 오늘 건너뛰기 후 창 파기 |
| `close_window()` | 저장 후 창 파기 |

### 5.2 사용 플러그인

- `tauri-plugin-global-shortcut` — 전역 단축키
- `tauri-plugin-autostart` — 로그인 시 자동 실행
- `tauri-plugin-notification` — Windows 토스트
- `tauri-plugin-opener` — config.json / 로그 폴더 열기
- `notify` (crate) — config.json 파일 감시

---

## 6. 구현 단계

각 단계는 독립적으로 동작 확인이 가능하도록 나눴다.

### Phase 0 — 스캐폴딩
- [ ] `npm create tauri-app` 으로 vanilla JS 템플릿 생성
- [ ] 앱 식별자 `com.dailyworkalter.app`, 창 제목 설정
- [ ] 아이콘 준비 (트레이용 16/32, 앱용 256)
- [ ] `npm run tauri dev` 로 빈 창이 뜨는 것 확인

### Phase 1 — 설정·저장 계층 (Rust)
- [ ] `config.rs` — 기본값 정의, 로드, 누락 키 병합, 저장
- [ ] `state.rs` — state.json 로드·저장, skippedDates 정리
- [ ] `storage.rs` — 로그 경로 계산, frontmatter 파싱·직렬화
- [ ] `read_week` — 주 시작일 계산(월요일 기준), 근무일만 조회
- [ ] `%APPDATA%` 경로 해석 및 최초 디렉터리 생성
- [ ] 단위 테스트: frontmatter 없는 파일, 빈 파일, 깨진 config 파싱

### Phase 2 — 트레이 + 창 생명주기
- [ ] 트레이 아이콘 + 메뉴 구성
- [ ] 창을 **파기·재생성**하는 함수 (`window.rs`)
- [ ] 창 닫기 = 파기, 앱은 트레이에 계속 상주
- [ ] 창 위치: 작업표시줄 위 우하단, 항상 위, 작업표시줄 미표시
- [ ] **검증: 창을 닫은 상태에서 작업관리자 메모리 20MB대 확인**

### Phase 3 — 스케줄러
- [ ] 60초 tick 루프 (별도 스레드)
- [ ] 3.1 판정 순서 구현
- [ ] `lastTickAt` 간격으로 절전 복귀 감지
- [ ] 스누즈 / 건너뛰기 처리
- [ ] 트레이 "지금 알림 테스트"로 시간을 기다리지 않고 검증

### Phase 4 — 일일 입력 화면
- [ ] 날짜 헤더, 불릿 textarea
- [ ] `- ` 자동 삽입, Enter 시 다음 줄 자동
- [ ] 500ms 디바운스 자동저장
- [ ] 어제 참고 접기·펼치기
- [ ] Ctrl+Enter / Esc 단축키
- [ ] 스누즈·건너뛰기·저장 버튼

### Phase 5 — 주간 요약 화면
- [ ] 주차 계산 및 헤더 (`2026년 37주차 (09-07 ~ 09-11)`)
- [ ] 요일별 목록 렌더, 미작성일 표시
- [ ] 과거 요일 인라인 편집
- [ ] 오늘 입력란 병행 배치
- [ ] 주간 전체 복사 (마크다운 서식)
- [ ] `fixedDay` / `lastWorkday` 모드 분기 확인

### Phase 6 — 설정 팝업
- [ ] 4.3 폼 전체 구현
- [ ] 시간 입력 검증 (출근 < 퇴근, 형식 오류 처리)
- [ ] 근무 요일 전체 해제 방지
- [ ] 단축키 변경 시 재등록 및 충돌 처리
- [ ] config.json / 로그 폴더 열기 버튼
- [ ] **config.json 파일 감시 → 외부 수정 시 자동 리로드**
- [ ] 파싱 실패 시 기존 설정 유지 + 알림

### Phase 7 — 온보딩 · 자동 실행 · 단축키
- [ ] 최초 실행 감지 → 온보딩 화면
- [ ] 자동 실행 등록·해제
- [ ] 전역 단축키 등록, 눌렀을 때 창 생성
- [ ] 알림 시각에 Windows 토스트 발송 → 클릭 시 창 생성

### Phase 8 — 빌드 · 배포
- [ ] `npm run tauri build`
- [ ] 산출물 크기·상주 메모리 실측 기록
- [ ] exe를 다른 폴더로 옮겨서 단독 실행 확인
- [ ] 첫 실행 시 AppData 생성 → 온보딩 → 트레이 상주 전 과정 확인
- [ ] 재부팅 후 자동 실행 확인

---

## 7. 검증 시나리오

시간이 지나야 확인되는 기능이 많으므로, 아래를 개발 중 반복 확인한다.

| 시나리오 | 기대 동작 |
|---|---|
| 알림 시각 도달 | 창이 뜨고 오늘 날짜로 입력 대기 |
| 이미 작성한 날 | 안 뜸 |
| 스누즈 후 10분 경과 | 다시 뜸 |
| 오늘 건너뛰기 | 그날은 다시 안 뜸, 다음날은 정상 |
| 알림 시각에 절전 → 2시간 뒤 복귀 | 복귀 직후 뜸 |
| 알림 시각에 절전 → 다음날 아침 복귀 | 안 뜸 (catchUpHours 초과 + 출근 전) |
| 토요일 | 안 뜸 |
| 지정 요일(금) | 주간 화면으로 뜸 |
| config.json 손으로 수정 | 앱 재시작 없이 반영 |
| config.json 깨뜨림 | 기존 설정 유지, 오류 알림 |
| 창 닫은 상태 메모리 | 20MB 내외 |
| 작성 중 앱 강제 종료 | 직전 입력까지 파일에 남아 있음 |

---

## 8. 이번 범위에서 제외 (향후 후보)

- **git 커밋 자동 수집** — 지정한 repo 폴더들에서 오늘 본인 커밋을 긁어 초안 생성.
  실사용률에 가장 크게 기여할 기능이므로 1순위 후보
- 어제 작성분 기반 이어쓰기 제안
- 태그(`#프로젝트명`)와 태그별 통계
- 월간 / 분기 요약
- 공휴일 달력 연동
- Jira 등 이슈 트래커 연동
- 백업 / 내보내기 (현재는 폴더 수동 복사)

---

## 9. 참고

- 데이터 경로: `%APPDATA%\DailyWorkAlter\`
- 완전 삭제: exe 삭제 + 위 폴더 삭제 + 자동 실행 레지스트리 항목 제거
- 로그는 순수 마크다운이므로 앱을 버려도 기록은 그대로 남는다
